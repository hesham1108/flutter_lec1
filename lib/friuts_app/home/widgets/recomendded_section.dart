import 'package:flutter/material.dart';
    
class RecomenddedSection extends StatelessWidget {

  const RecomenddedSection({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}